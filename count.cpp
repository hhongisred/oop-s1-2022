#include<iostream>

extern int count(int array[], int n);

int count() {
    for (int i = 0; 
    i < n;
    i ++) {
        if (A[i]%2 == 0)
        count ++;
    }
    std::cout << "The number is: " << i << std::endl;
    return count;
}
#include<iostream>
using namespace std;

int count(int array[], int n, int number)
{
    for (int i = 0; i < n;)
    {
        if (array[i] = number)
        {
            return number;
        }

    }

    if ( n < 1)
        {
        return 0;
        }
    return number;
}